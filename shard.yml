name: carbon
version: 0.1.0

authors:
  - Benoist Claassen <benoist.claassen@gmail.com>

license: MIT

scripts:
  postinstall: if ![ -f "./carbon" ]; then ; crystal build src/command.cr -o ./carbon ; fi
